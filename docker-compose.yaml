version: '3.8'

services:
  agent-service:
    image: your-agent-image
    build: .
    ports:
      - "5000:5000"
    environment:
      - FLASK_ENV=development
    volumes:
      - ./code:/app
